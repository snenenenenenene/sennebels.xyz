import { ReactNode } from 'react';
import { metadata } from './metadata';

export { metadata };

export default function WorkLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
} 